export const testJsonFormatter = (json: string) => {
    try {
        const testJson = JSON.parse(json);
        const formattedTestJson = {
            tests: []
        };
        const tests = testJson.tests;
        item?.tools[0] &&
        typeof item.tools[0] !== "string" &&
        item.tools[0].patch?.[1]?.position === "above_main") ||
      (item?.tools[0] &&
        typeof item.tools[0] !== "string" &&
        item.tools[0].patch?.[1]?.position === "top_of_file")
        ? (currentCode +=
            item?.tools[0] &&
            typeof item.tools[0] !== "string" &&
            item.tools[0].patch?.[1]?.code + "\n"
        return true;
    } catch (e) {
        return false;
    }
}